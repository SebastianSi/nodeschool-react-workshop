import React from 'react';

function RepositoriesPage() {
    return (
        <h2>Repositories Page will be here</h2>

        //TODO: Wohoo, our 1st task (basic props):
        // Let's add a header component and a RepositoriesContainer component like this:
        // <RepositoriesHeader />
        // <RepositoriesContainer someProp={"Greetings from your parent"}/>
        // As you see, we passed a prop called someProp to RepositoriesContainer;
        // We'll want to create these 2 components (RepositoriesHeader and RepositoriesContainer)
        // similar to this component we're in, but changing the <h2> in RepositoriesContainer
        // so that it displays the value we're passing it with the someProp prop
    );
}

export default RepositoriesPage;